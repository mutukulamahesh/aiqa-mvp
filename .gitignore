node_modules/
dist/
build/
.env
.DS_Store

# AIQA runtime data — run outputs, healer cache, screenshots
.aiqa/

# Scratch / export files
conversation_export.md
exploration.json
ARCHITECTURE.html

# RWT runtime data — generated SQLite DB (recreated by setup-rwt-db.ts)
rwt-data/

# Runtime results — logs, uptime, screenshots (not source artefacts)
results/
