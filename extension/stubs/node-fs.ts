// Browser stub for Node.js 'fs' module.
// DslParser.ts imports fs but only uses it in parseTestFile(), which is never
// called from the extension. This stub makes the bundler happy without
// including any Node.js runtime code.
export {};
